# MailPilot 发布约定

- 用户确认正式版本从 **1.0.0** 归档；此前 1.8.x 仅保留为开发历史，不再作为新版本基线。
- 当前测试版为 **1.0.0（43）**；用户验收后才放行 **1.0.1**。设置支持简体中文、繁体中文、英语、日语、韩语、西班牙语、法语、德语，默认简体中文，语言选择持久保存。界面资源使用 ARB，不翻译用户资料或业务标识。保留详情页下方的会话位置、输入和展开状态；邮件检索结果以两封预览、每页五封的可展开卡片呈现，阅读时暂停自动跟随。被覆盖的编辑器不处理返回事件。仅自动 GET，网页缓存策略仍为 v3；保留动态读取及引用选区高亮、链接发现、DSML／Seed 工具适配、参数校验、七轮收集与最终回答、会话隔离及发送确认。设置页保留版本号和发送确认提示，不展示构建号；Room 保持 v9。
- `pubspec.yaml` 是版本来源。Android `versionCode` 持续递增，不随正式版本归档重置；1.0.0 对应构建号 15，1.0.1 对应构建号 16，1.0.2 首次交付对应构建号 17；按用户明确要求，版本栏间距调整后仍打包为 1.0.2，内部构建号从 18 递增至 19。
- 沿用原发布签名。设置页通过平台返回的 APK 构建信息展示实际版本，不单独写死版本号。
- 签名材料不进入源码包；覆盖升级验证不得删除用户数据。模拟器测试复用已有系统镜像，完成后只清理本轮临时设备。
- 禁止对日常设备的 `app.mailpilot` 执行 `flutter drive`：它默认在结束时卸载应用。本轮测试事故已记录于构建 33 说明。UI 集成测试必须使用 `scripts/Test-Profile.ps1` 的隔离包名与 `--keep-app-running`；发布覆盖升级只用 `adb install -r`，先备份再校验，不以卸载解决签名冲突。

- 归档历史：2026-09-09 Agent 修复先统一为 **1.0.0（20）**，此前拟定的 1.0.3 未作为正式交付版本；摘要阅读修复为 **1.0.0（21）**，上下文与邮箱配置更新为 **1.0.0（22）**，聊天直达最新消息为 **1.0.0（23）**。
