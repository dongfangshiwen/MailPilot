# MailPilot 1.0.0（28）· 删除按钮与设置页脚

此前只更换配色，删除图标仍使用自绘线条，因而形状没有符合用户给出的示例。本轮直接采用用户提供的 DeepSeek SVG 原始路径：保留 16 × 16 坐标、全部曲线与填充轮廓，转换为缓存的 Flutter Path，不新增图标依赖。

删除图标和文字统一使用用户开发者工具截图中的 `--dsw-alias-state-error-primary` 值 `#EC1313`，深浅色均使用该指定色。长按菜单、大字体操作抽屉及多选底栏共用此配色与图标；删除图标为 16 dp，按钮点击范围继续至少 48 dp。保留置顶样式、禁用状态以及删除前确认。

设置页底部显示 `MailPilot 1.0.0`，第二行保留“每次发送邮件，都由你确认。”。不显示构建号；正式版本号仍动态读取 APK 信息，未在页面硬编码。内部构建号仅用于覆盖升级和诊断。

正式版本仍为 1.0.0，内部构建号由 27 递增至 28，沿用原发布签名，Room v9。本轮仅调整 Flutter 显示，未修改邮件、模型、缓存或发送流程。

验证包括 Flutter 静态检查、现有侧栏 32 项与设置 18 项回归（320／360／412 dp，1.0／1.6／2.0 字体及深浅主题）、实际组件渲染截图、APK 签名、版本、对齐与包内源码校验。截图位于 `docs/screenshots/build28`，使用本地样例资料与主机中文字体渲染，不属于真机截图。本轮没有新增设备覆盖安装或真实邮箱／模型测试。

APK、源码、Windows 脚本及完整交付包在 `artifacts`，附 SHA-256。验证见 [记录](test-evidence/build28/validation.json)。本轮没有创建临时设备；保留正在使用的调试构建缓存，缓存与本地签名材料不进入交付包。
