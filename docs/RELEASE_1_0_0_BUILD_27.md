# MailPilot 1.0.0（27）· 暖朱砂红

用户截图中的主要红色像素为 `RGB(182,79,88)`，即构建 26 的 `#B64F58`。上一轮配色已经生效，但仍偏玫红，视觉变化不符合用户预期。

本次浅色主题改为暖朱砂红 `#C44736`，深色主题使用 `#EC9282`。长按菜单、大字体操作抽屉和多选底栏的删除图标及文字继续复用同一配色函数。保留线框图标、点击区域和删除确认逻辑。

设置页底部仅显示 `MailPilot 1.0.0`，版本号仍取自 APK 的实际 `versionName`。移除构建号及额外提示文字；内部 `versionCode` 继续递增，用于正常覆盖升级。

正式版本继续保持 1.0.0，构建号 26 → 27，沿用原签名和 Room v9。本轮为界面显示调整，静态检查、现有侧栏 32 项及设置页 18 项回归通过，并运行签名和文件校验；不重复调用邮箱、模型或 SMTP，不操作用户正在运行的调试设备。没有新增设备覆盖安装测试。

Windows 构建及打包脚本继续位于 `scripts`；源码、APK、完整交付包均在 `artifacts` 并附 SHA-256。验证见 [记录](test-evidence/build27/validation.json)。为保留用户当前 Android Studio 调试会话，本轮不运行 `flutter clean`，构建目录作为正在使用的调试缓存保留；缓存不进入交付包。
