# MailPilot 1.0.0（26）· 删除配色调整

按用户要求继续使用 1.0.0，内部构建号由 25 递增至 26，沿用原包名和签名，可直接覆盖安装。

历史对话的删除图标和文字统一改为柔和的豆沙红：浅色主题 `#B64F58`，深色主题 `#E39A9E`。长按菜单、大字体下的操作抽屉及多选底栏复用同一配色。保留简洁线框图标和删除确认流程。

本轮只调整界面配色与发布版本，Room 保持 v9。构建 25 的图文请求、图片复用及侧栏功能继续保留；其真实模型和设备验证记录仍归属于构建 25，本轮不重复调用邮箱、模型或 SMTP，也不新增测试设备。

重新运行静态检查及现有侧栏回归，核对 APK 的版本、签名、ZIP 对齐和 SHA-256。最新验证与缓存清理记录见 [验证记录](test-evidence/build26/validation.json)。本轮未另做设备覆盖安装测试。

构建后使用 `flutter clean` 清理项目构建缓存，保留用户邮件、聊天、有效图片分析缓存、发布签名、已有系统镜像及日常设备。源码包和完整交付包排除 `.tools`、本地配置与签名材料。

交付位于 `artifacts`：APK、源码 ZIP、完整交付 ZIP 均附同名 `.sha256` 文件。Windows 构建和打包脚本继续位于 `scripts`。
